import React from 'react';
import { Layout } from 'antd';
import styles from './Footer.module.css';

const Footer = () => (
  <Layout.Footer className={styles.wrapper}>Footer placeholder</Layout.Footer>
);

export default Footer;
